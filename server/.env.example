# Copy to .env and add your Railway Postgres URLs
# Local dev: use DATABASE_PUBLIC_URL (from Railway → database → Variables)
# On Railway: DATABASE_URL is auto-set for internal network
DATABASE_PUBLIC_URL=postgresql://postgres:YOUR_PASSWORD@HOST.rlwy.net:PORT/railway
